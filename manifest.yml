applications:
- name: spring-petclinic
  memory: 4Gi
  instances: 1
  env:
    spring.profiles.active: mysql
  volume_mounts:
    - driver: nfs
      container_dir: /data
      device:
        volume_id: data
